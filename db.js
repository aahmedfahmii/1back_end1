const sqlite = require ('sqlite3')
const db = new sqlite.Database ('database.db')

const createUserTable= `CREATE TABLE IF NOT EXISTS USER (ID INTEGER PRIMARY KEY AUTOINCREMENT,
NAME TEXT NOT NULL,
EMAIL TEXT UNIQUE NOT NULL,
PASSWORDTEXT NOT NULL,    
ISADMIN INT)`

const createFieldTable= `CREATE TABLE IF NOT EXISTS FLIGHT (ID INTEGER PRIMARY KEY AUTOINCREMENT,
NAME TEXT NOT NULL,
TIME INT NOT NULL,
DATE TEXT NOT NULL,    )`

Xxxxxxxxxxx